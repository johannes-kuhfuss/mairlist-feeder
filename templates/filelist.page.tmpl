{{ define "filelist.page.tmpl" }}

{{ template "header" .}}

   <div class="container-fluid py-5">
        <div class="row">
            <div class="col">
                <style>
                    .sortable-header {
                        align-items: center;
                        background: none;
                        border: 0;
                        color: inherit;
                        display: inline-flex;
                        font: inherit;
                        gap: 0.35rem;
                        padding: 0;
                        text-align: left;
                    }

                    .sortable-header:hover,
                    .sortable-header:focus {
                        color: var(--bs-link-hover-color);
                        text-decoration: underline;
                    }

                    .sort-indicator {
                        display: inline-block;
                        min-width: 0.75rem;
                    }
                </style>

                <table class="table table-striped table-sm" id="filelist-table">
                    <thead>
                        <tr>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="0" data-sort-type="text">Path <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="1" data-sort-type="text">Folder Date <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="2" data-sort-type="text">Start Time <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="3" data-sort-type="text">End Time <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="4" data-sort-type="number">Duration (min) <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="5" data-sort-type="text">Modification Time <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="6" data-sort-type="text">Scan Time <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="7" data-sort-type="text">Rule Matched <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="8" data-sort-type="number">EventId <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="9" data-sort-type="text">CalCMS (from, enriched, title) <span class="sort-indicator" aria-hidden="true"></span></button></th>
                          <th scope="col"><button class="sortable-header" type="button" data-sort-column="10" data-sort-type="text">Technical Metadata (Bitrate, Format) <span class="sort-indicator" aria-hidden="true"></span></button></th>
                        </tr>
                    </thead>
                    <tbody>
                        {{ range .files }}
                        <tr>
                          <td>{{ .Path }}</td>
                          <td>{{ .FolderDate }}</td>
                          <td>{{ .StartTime }}</td>
                          <td>{{ .EndTime }}</td>
                          <td>{{ .Duration }}</td>
                          <td>{{ .ModTime }}</td>
                          <td>{{ .ScanTime }}</td>
                          <td>{{ .RuleMatched }}</td>
                          {{ if .EventLinkAvail }}
                            <td><a href="{{ .EventIdLink }}" target="_blank" rel="noopener noreferrer">{{ .EventId }}</a></td>
                          {{ else }}
                            <td>"N/A"</td>
                          {{ end }}
                          <td>{{ .CalCmsInfo }}</td>
                          <td>{{ .TechMd }}</td>
                        </tr>
                        {{ end }}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

<script>
  (function () {
    const table = document.getElementById("filelist-table");
    if (!table) {
      return;
    }

    const tbody = table.querySelector("tbody");
    const headers = table.querySelectorAll(".sortable-header");
    let activeColumn = null;
    let activeDirection = "asc";

    function getCellValue(row, columnIndex, sortType) {
      const cell = row.cells[columnIndex];
      const text = cell ? cell.textContent.trim().replace(/^"|"$/g, "") : "";

      if (sortType === "number") {
        const value = Number.parseFloat(text);
        return Number.isNaN(value) ? Number.NEGATIVE_INFINITY : value;
      }

      return text.toLocaleLowerCase();
    }

    function updateHeaderState(activeHeader) {
      headers.forEach(function (header) {
        const indicator = header.querySelector(".sort-indicator");
        const isActive = header === activeHeader;
        header.closest("th").setAttribute("aria-sort", isActive ? activeDirection : "none");
        indicator.textContent = isActive ? (activeDirection === "asc" ? "^" : "v") : "";
      });
    }

    headers.forEach(function (header) {
      header.addEventListener("click", function () {
        const columnIndex = Number.parseInt(header.dataset.sortColumn, 10);
        const sortType = header.dataset.sortType;

        if (activeColumn === columnIndex) {
          activeDirection = activeDirection === "asc" ? "desc" : "asc";
        } else {
          activeColumn = columnIndex;
          activeDirection = "asc";
        }

        const rows = Array.from(tbody.querySelectorAll("tr"));
        rows.sort(function (rowA, rowB) {
          const valueA = getCellValue(rowA, columnIndex, sortType);
          const valueB = getCellValue(rowB, columnIndex, sortType);
          let comparison;

          if (sortType === "number") {
            comparison = valueA - valueB;
          } else {
            comparison = valueA.localeCompare(valueB, undefined, { numeric: true, sensitivity: "base" });
          }

          return activeDirection === "asc" ? comparison : -comparison;
        });

        rows.forEach(function (row) {
          tbody.appendChild(row);
        });
        updateHeaderState(header);
      });
    });
  })();
</script>

{{ template "footer" .}}

{{ end }}
